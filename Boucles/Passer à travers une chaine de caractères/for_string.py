hello_world = "Hello, World!"

for character in hello_world:    # Imprime chaque caractères de hello_world
    print(character)

length = 0      # Initialize length variable

# Iteration sur une chaine.
    length += 1     # Ajoute 1 à la longueur à chaque itération pour compter les caractères

print(len(hello_world) == length)
