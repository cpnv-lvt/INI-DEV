def sum_two_numbers(a, b):
    return a + b  # Retour le résultat à la fonction appelante


c = sum_two_numbers(3, 12)  # Assigne le résultat de la fonction à c


def fib(n):
    """Ceci est la documentation de la fonction. Elle est disponible via fib.__doc__()
    Retourne une liste contenant la série de Fibonacci jusqu^à n."""
    result = []
    a = 1
    b = # Assigne une valeur initiale à b
    while a < n:
        result.append(a)
        tmp_var = b
        # Mise à jour de certaine valeur avec sum
        # Restoration de la variable avectemp
    # Retour le résultat


print(fib(10))
